# 城北中央公園テニスコート予約アプリ　セットアップ手順書

---

## はじめに

この手順書では、アプリを使えるようにするまでの手順を説明します。  
所要時間：約15〜20分（初回のみ）

---

## ステップ1：Pythonをインストールする

1. ブラウザで以下のURLを開く  
   👉 https://www.python.org/downloads/

2. **「Download Python 3.x.x」** ボタンをクリックしてダウンロード  
   ⚠️ **Python 3.12 または 3.13 を使うこと。3.14以降は対応パッケージがなく動作しません。**  
   バージョンを確認して、3.14以降の場合は画面下の「Looking for a specific release?」から **Python 3.13.x** を選んでダウンロードしてください。

3. ダウンロードしたファイルをダブルクリックして起動

4. **「Add python.exe to PATH」** に必ずチェックを入れる（重要）

5. **「Install Now」** をクリックして待つ

6. 「Setup was successful」と表示されたら **「Close」** をクリック

---

## ステップ2：アプリをダウンロードする

1. ブラウザで以下のURLを開く  
   👉 https://github.com/Laissez-Faire-TC/Court_Reservation

2. 緑色の **「Code」** ボタンをクリック

3. **「Download ZIP」** をクリック

4. ダウンロードが完了したら、ダウンロードフォルダにZIPファイルが保存される

---

## ステップ3：ZIPファイルを解凍する

1. ダウンロードした **ZIPファイルを右クリック**

2. **「すべて展開」** をクリック

3. **「展開」** ボタンをクリック

4. 展開されたフォルダが開く（`Court_Reservation-main` という名前）

---

## ステップ4：必要なソフトをインストールする

1. 展開したフォルダの中を開く

2. フォルダ内の何もない場所で **Shiftキーを押しながら右クリック**

3. **「PowerShellウィンドウをここで開く」** または **「ターミナルで開く」** をクリック

4. 黒い画面が開いたら、以下をコピーして貼り付け、Enterを押す

```
pip install -r requirements.txt
```

5. インストールが完了するまで待つ（数分かかります）

---

## ステップ5：アプリを起動する

先ほどの黒い画面で、以下をコピーして貼り付け、Enterを押す

```
python johoku_app.py
```

アプリが起動します。

---

## 2回目以降の起動方法

毎回ステップ4〜5を繰り返すのは面倒なので、デスクトップにショートカットを作ることができます。

1. エクスプローラーを開き、上部メニューの **「表示」** をクリック

2. **「ファイル名拡張子」** にチェックを入れる（これにより `.txt` などの拡張子が表示されるようになる）

3. フォルダ内で右クリック → **「新規作成」** → **「テキストドキュメント」**

4. 作成された `新しいテキスト ドキュメント.txt` をメモ帳で開いて以下を貼り付けて保存する

```
@echo off
cd /d "%~dp0"
python johoku_app.py
pause
```

5. ファイルを閉じたら、ファイル名を `起動.bat` に変更する（`.txt` を削除して `起動.bat` と入力）

6. 「拡張子を変更すると、ファイルが使えなくなる可能性があります」と表示されたら **「はい」** をクリック

7. **`起動.bat` はアプリフォルダから移動しないこと**（移動するとエラーになる）

8. デスクトップから起動したい場合は、**Altキーを押しながら `起動.bat` をデスクトップにドラッグ**するとショートカットが作成される

9. 次回から作成したショートカットをダブルクリックするだけで起動できます

---

## うまくいかない場合

| 症状 | 対処法 |
|------|--------|
| `python` が認識されない | Pythonインストール時に「Add to PATH」にチェックしたか確認。PCを再起動して再試行 |
| `pip install` でエラーが出る | PCがインターネットに接続されているか確認 |
| アプリが起動しない | PCを再起動してから再度試してください |
| それでも解決しない | サポート担当者（渡邉：080-2671-9571）に連絡してください |

---

*Laissez-Faire T.C.*
